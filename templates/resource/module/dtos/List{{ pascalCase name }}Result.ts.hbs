import { Field, ObjectType } from '@nestjs/graphql';
import { IGenericSearchResult } from '../../../../common/zod/GenericSearchInputZod';
import { {{ pascalCase name }}Type } from '../{{ paramCase name }}.type';


@ObjectType('List{{ pascalCase name }}Result')
export class List{{ pascalCase name }}ResultType extends GenericSearchResultType<{{ pascalCase name }}Type | null> {
  @Field(() => [{{ pascalCase name }}Type], { nullable: 'items' })
  items!: ({{ pascalCase name }}Type | null)[];
}
