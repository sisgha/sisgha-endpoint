import { Field, ObjectType } from '@nestjs/graphql';
import { GenericSearchResultType } from 'src/meilisearch/dtos';
import { {{ pascalCase name }}Type } from '../{{ paramCase name }}.type';

@ObjectType('List{{ pascalCase name }}Result')
export class List{{ pascalCase name }}ResultType extends GenericSearchResultType<{{ pascalCase name }}Type> {
  @Field(() => [{{ pascalCase name }}Type])
  items!: {{ pascalCase name }}Type[];
}
