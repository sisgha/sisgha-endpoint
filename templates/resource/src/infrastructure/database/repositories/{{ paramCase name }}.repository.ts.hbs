import { DataSource, EntityManager } from 'typeorm';
import { {{ pascalCase name }}DbEntity } from '../entities/{{ snakeCase name }}.db.entity';

export type I{{ pascalCase name }}Repository = ReturnType<typeof get{{ pascalCase name }}Repository>;

export const get{{ pascalCase name }}Repository = (dataSource: DataSource | EntityManager) =>
  dataSource.getRepository({{ pascalCase name }}DbEntity).extend({
    // ...
  });

